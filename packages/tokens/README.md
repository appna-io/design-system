# @apx-ds/tokens

Static design token definitions: palettes (light/dark), spacing, radii, shadows, motion, typography,
breakpoints, and theme variants for `apx-ds`.

Pure data, no React, no runtime — safe for non-React tooling (Tailwind configs, Figma sync, etc.).