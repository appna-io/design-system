import type { BreakpointScale } from '@apx-ui/engine';

/** Breakpoint widths in px. Match Tailwind defaults so utility classes line up with DS responsive props. */
export const breakpoints: BreakpointScale = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1536,
};
