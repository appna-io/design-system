# @apx-ds/theme

Runtime theme system: `ThemeProvider`, hooks (`useTheme`, `usePalette`, `useMode`, `useVariant`,
`useDirection`), CSS-variable generation, theme override resolution, and the Tailwind preset.