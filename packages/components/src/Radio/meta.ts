import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'radio',
  displayName: 'Radio',
  description:
    'Single-of-many selection control shipped with its `<RadioGroup />` parent. Same hidden-input + custom-indicator pattern as Checkbox/Switch, with the inner filled dot rendered as the control\'s ::before pseudo-element for the cleanest DOM. Three variants × seven colors × three sizes; vertical / horizontal orientation; group-level defaults propagated via context.',
  category: 'Inputs',
  tags: ['form', 'selection', 'radio', 'radiogroup'],
};