import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'card',
  displayName: 'Card',
  description:
    'Compound content container. Root + Header / Body / Footer / Media / Divider subparts, with optional whole-card click, hover lift, selection ring, and horizontal "media on the side" layout. The reference pattern for every future compound component in the DS.',
  category: 'Surfaces',
  tags: ['card', 'container', 'layout', 'compound', 'surface'],
};