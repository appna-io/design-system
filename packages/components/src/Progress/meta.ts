import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'progress',
  displayName: 'Progress',
  description:
    'Progress indicators in two shapes — `<Progress />` (linear) and `<CircularProgress />` (SVG ring). CSS-only animations (no Motion library), determinate + indeterminate modes, seven palette colors, three sizes, and `prefers-reduced-motion` support across both surfaces.',
  category: 'Feedback',
  tags: ['progress', 'loading', 'indicator', 'spinner', 'circular'],
};
