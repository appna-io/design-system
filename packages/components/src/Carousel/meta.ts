import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'carousel',
  displayName: 'Carousel',
  description:
    "Accessible, keyboard- and touch-friendly carousel powered by CSS scroll-snap with a thin JS coordination layer. Zero new dependencies (no Embla / Swiper). Autoplay off by default; when enabled, always-visible pause/play (WCAG 2.2.2). Horizontal + vertical, responsive `slidesPerView`, opt-in loop, smart auto-wrap defaults, and a full compound API for power users.",
  category: 'Navigation',
  tags: ['carousel', 'slider', 'gallery', 'lane', 'scroll-snap', 'compound', 'a11y', 'autoplay'],
};
