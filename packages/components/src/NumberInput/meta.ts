import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'number-input',
  displayName: 'NumberInput',
  description:
    'Numeric form control. Typed `number | null` value, locale-aware formatting and parsing, clamping, precision rounding, accelerating mouse-hold stepper, full keyboard control, and hidden-input form submission with the canonical value.',
  category: 'Inputs',
  tags: ['form', 'number', 'numeric', 'input', 'spinbutton', 'currency'],
};