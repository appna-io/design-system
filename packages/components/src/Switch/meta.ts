import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'switch',
  displayName: 'Switch',
  description:
    'Binary on/off toggle. Hidden-input + sliding-thumb pattern. CSS-only slide (no Motion lib), 3 variants × 7 colors × 2 shapes × 3 sizes, async-friendly `loading` state, optional per-state thumb glyphs. The companion to Checkbox for instant-effect settings.',
  category: 'Inputs',
  tags: ['form', 'toggle', 'boolean', 'switch'],
};