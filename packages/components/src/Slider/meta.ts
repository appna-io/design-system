import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'slider',
  displayName: 'Slider',
  description:
    'Numeric range input — single or N-thumb. Horizontal + vertical orientation, optional marks + value bubble, full W3C Slider keyboard. Pure-CSS thumb animation (no Motion library). Hidden `<input type="range">` for form submission.',
  category: 'Inputs',
  tags: ['form', 'slider', 'range', 'number'],
};