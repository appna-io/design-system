import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'select',
  displayName: 'Select',
  description:
    "Form-control combobox — the intersection of `<Input>`'s shell and `<Menu>`'s listbox. Single-select dropdown with W3C Listbox + Combobox ARIA pattern, type-ahead, keyboard nav, hidden `<input>` for form submission, and 4 × 7 form-control variant matrix matching Input/Textarea heights per size.",
  category: 'Forms',
  tags: ['select', 'combobox', 'dropdown', 'form-control', 'listbox'],
};
