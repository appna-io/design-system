import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'avatar',
  displayName: 'Avatar',
  description:
    'User-identity glyph with image, initials, and icon fallback. Ships with deterministic name → color hashing, status indicator, accent ring, and an AvatarGroup that overlaps children into a stack with an optional `+N` overflow tile.',
  category: 'Data Display',
  tags: ['avatar', 'user', 'profile', 'identity', 'group', 'stack'],
};