import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'div',
  displayName: 'Div',
  description:
    'Styling primitive ("Box"). A `<div>` with built-in CSS shorthand props (inline-style based, token-aware), responsive show/hide (`hideOn` / `displayOn`), motion presets (`animation`), `centered` shortcut, polymorphic `actLike` / `asChild`, full HTML pass-through, and pseudo-state className hooks (`onHover`, `onFocusVisible`, `onActive`, ...).',
  category: 'Layout',
  tags: ['div', 'box', 'layout', 'styling', 'primitive', 'polymorphic', 'responsive', 'animation'],
};
