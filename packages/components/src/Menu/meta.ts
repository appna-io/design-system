import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'menu',
  displayName: 'Menu',
  description:
    "Dropdown / context / hover menu primitive — Popover's keyboard-driven cousin. 13-subpart compound supporting Items, CheckboxItems, RadioGroups, Submenus, and four UX patterns (action menu / multi-select / single-select / hierarchy) under one API. Single `trigger` prop unifies DropdownMenu + ContextMenu + HoverMenu.",
  category: 'Overlays',
  tags: ['menu', 'dropdown', 'context-menu', 'overlay'],
};