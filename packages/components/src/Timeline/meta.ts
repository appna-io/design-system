import type { ComponentMeta } from '@apx-ui/engine';

export const meta: ComponentMeta = {
  name: 'timeline',
  displayName: 'Timeline',
  description:
    'Chronological event list for audit logs, activity feeds, order tracking, and version history. Vertical / horizontal / alternating layouts, five tones + active emphasis, locale-aware relative + absolute timestamps via `Intl.RelativeTimeFormat`, optional collapsible items with proper ARIA disclosure.',
  category: 'Data Display',
  tags: ['timeline', 'feed', 'activity', 'history', 'audit', 'log', 'order', 'tracking'],
};