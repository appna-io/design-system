/**
 * `templates/_kit/mockups` — the frames templates show their product in.
 *
 * Three things, not the eight #5 proposed, because three real mockups existed to design against
 * and they said the frame is the reusable part while the content is not. See `MockFrame`'s note
 * for the reasoning and for the rule that governs adding a fourth.
 */
export { MockFrame } from './MockFrame';
export type { MockFrameChrome, MockFrameProps } from './MockFrame';
export { CodeMock } from './CodeMock';
export type { CodeMockProps } from './CodeMock';
export { TerminalMock } from './TerminalMock';
export type { TerminalMockProps } from './TerminalMock';
