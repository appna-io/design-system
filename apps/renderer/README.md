# @apx-ds/renderer

Local development preview app for the `apx-ds` component library. This app is **not published**;
it lives in the workspace for development only.

```bash
pnpm dev              # from workspace root — starts this app at localhost:3000
pnpm build:preview    # produces a static build of the renderer
```